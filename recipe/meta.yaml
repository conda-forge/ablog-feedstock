{% set name = "ablog" %}
{% set version = "0.10.5" %}
{% set sha256 = "c69cbd34f8bee3166b40c89ad3ced2e4dac880a951619372f509d8729af8ce59" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  entry_points:
    - ablog = ablog.commands:ablog_main
  skip: true  # [py27]

requirements:
  host:
    - python
    - pip
    - setuptools_scm
  run:
    - python
    - werkzeug <1.0.0
    - sphinx >=2.0
    - alabaster
    - invoke
    - python-dateutil
    - sphinx-automodapi
    - watchdog

test:
  requires:
    - git
    - nbsphinx <0.5.0
  commands:
    - ablog --version
    - git clone --depth 1 --branch v{{ version }} https://github.com/sunpy/ablog
    - cd ablog/docs
    - ablog build -T
    - ablog build -b latex -T -d .doctrees -w _latex
    - ablog build -T -b json
    - ablog build -T -b pickle

about:
  home: https://github.com/sunpy/ablog
  license: MIT
  license_file: LICENSE.rst
  license_family: MIT
  summary: ABlog for blogging with Sphinx
  description: |
    A Sphinx extension that converts any documentation or personal website
    project into a full-fledged blog.
  doc_url: http://ablog.readthedocs.org
  dev_url: https://github.com/sunpy/ablog

extra:
  recipe-maintainers:
    - nabobalis
    - cadair
